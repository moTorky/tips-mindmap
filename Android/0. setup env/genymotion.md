provides some functions such set location, entire proxy (all network connections icmp,ssh,..)
- use `telnet 127.0.0.1 5554`
	- set location using `geo fix`
- send sms, virtual call
- set   proxy from genymotion setting
- 